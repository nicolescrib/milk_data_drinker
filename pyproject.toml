[project]
name = "milk_data_drinker"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "pandas",
    "openpyxl",
    "xlrd",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
